export type Platform = {
  created_at: string;
  created_by: number | null;
  id: number;
  is_active: boolean;
  name: string;
  updated_at: string;
  updated_by: number | null;
};

export default Platform;
